

def make_coin_change ( coins, total,  partial_sum,  index, coin_used):
    #mean we have processed all coins
    if index> len(coins):
        return

    if partial_sum
    if partial_sum==total:
        partial_sum=partial_sum+1
        make_coin_change(coins, )


if __name__=='__main__':
    pass
else:
    pass
